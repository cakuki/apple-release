source "https://rubygems.org"
gem "fastlane"
# Danger powers the reusable PR-checks job (.github/workflows/danger.yml,
# EPIC-05 slice 3). Like fastlane, the exact version is pinned via Gemfile.lock
# rather than here, so `bundle update danger` is the single bump point.
gem "danger"
